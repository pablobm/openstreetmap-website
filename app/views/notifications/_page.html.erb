<%# locals: (notifications:, params:) %>

<turbo-frame id="pagination" target="_top" data-turbo="false">
  <% UserNotification.wrap(notifications.items).each do |notification| %>
    <%= render :partial => notification.to_partial_path, :object => notification, :as => :notification %>
    <hr>
  <% end %>

  <%= render "shared/pagination", :paginator => notifications, :params => params %>
</turbo-frame>
